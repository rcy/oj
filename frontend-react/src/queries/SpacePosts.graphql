query SpacePosts($spaceId: UUID!, $limit: Int) {
  posts(
    condition: { spaceId: $spaceId }
    last: $limit
    orderBy: CREATED_AT_ASC
  ) {
    edges {
      node {
        id
        body
        membership {
          id
          person {
            id
            name
            avatarUrl
          }
        }
      }
    }
  }
}
