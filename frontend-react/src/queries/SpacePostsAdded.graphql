subscription SpacePostsAdded($spaceId: UUID!) {
  posts(spaceId: $spaceId) {
    event
    post {
      id
      body
      membership {
        id
        person {
          id
          name
          avatarUrl
        }
      }
    }
  }
}
