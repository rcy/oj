query CurrentUserFamily {
  currentUser {
    id
    name
    family {
      id
      familyMemberships(orderBy: CREATED_AT_ASC) {
        nodes {
          id
          person {
            id
            name
            avatarUrl
          }
          role
        }
      }
    }
  }
}
