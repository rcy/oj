query CurrentUserFamily {
  currentUser {
    id
    name
    family {
      id
      familyMemberships(orderBy: _ASC) {
        nodes {
          id
          person {
            id
            name
          }
          role
        }
      }
    }
  }
}
